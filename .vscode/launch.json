{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "ng serve",
      "type": "msedge",
      "request": "launch",
      "preLaunchTask": "npm: dev",
      "url": "http://localhost:4200/",
      // "webRoot": "${workspaceFolder}"
    }
  ]
}
